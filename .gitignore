#ignore .env
.env
__pycache__

# Large / local artifacts (do not commit)
*.tar
*.tar.gz
.DS_Store

# Accidental nested clone of another repo (e.g. OneView) inside multi-agent-mcp
multi-agent-mcp/OneView/