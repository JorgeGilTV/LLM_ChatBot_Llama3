<dashboard version="1.0" theme="dark">
  <label>Jobs: not finished after 1 hour</label>
  <description>Lists jobs with a start event but no completed/failed event, with runtime over 3600s. Adjust the form (index, sourcetype) and edit the SPL if your fields are not job_id and status.</description>

  <fieldset submitButton="false" autoRun="true">
    <input type="text" token="idx" searchWhenChanged="true">
      <label>Index</label>
      <default>*</default>
    </input>
    <input type="text" token="st" searchWhenChanged="true">
      <label>Sourcetype</label>
      <default>*</default>
    </input>
  </fieldset>

  <!-- Row 1: stale job count -->
  <row>
    <panel>
      <title>Jobs not completed (&gt; 1 h since start)</title>
      <single>
        <search>
          <query>index=$idx$ sourcetype=$st$ earliest=-48h
( status="started" OR status="completed" OR status="failed" )
| stats earliest(eval(if(status="started",_time,null()))) AS start_t latest(eval(if(match(status,"completed|failed"),_time,null()))) AS end_t BY job_id
| where isnotnull(start_t) AND isnull(end_t)
| eval runtime_sec = now() - start_t
| where runtime_sec &gt; 3600
| stats count AS stale_jobs</query>
          <earliest>-48h</earliest>
          <latest>now</latest>
        </search>
        <option name="colorMode">block</option>
        <option name="field">stale_jobs</option>
        <option name="rangeValues">["0","1","5","20"]</option>
        <option name="rangeColors">["0x53a051","0xf2b827","0xe67e22","0xdc4e41"]</option>
        <option name="useColors">1</option>
      </single>
    </panel>
  </row>

  <!-- Row 2: detail table -->
  <row>
    <panel>
      <title>Detail: pending jobs &gt; 1 h</title>
      <table>
        <search>
          <query>index=$idx$ sourcetype=$st$ earliest=-48h
( status="started" OR status="completed" OR status="failed" )
| stats earliest(eval(if(status="started",_time,null()))) AS start_t latest(eval(if(match(status,"completed|failed"),_time,null()))) AS end_t BY job_id
| where isnotnull(start_t) AND isnull(end_t)
| eval runtime_sec = now() - start_t
| where runtime_sec &gt; 3600
| eval runtime_min = round(runtime_sec/60,1)
| eval start_human=strftime(start_t,"%Y-%m-%d %H:%M:%S %Z")
| table job_id start_human runtime_min runtime_sec
| sort - runtime_sec</query>
          <earliest>-48h</earliest>
          <latest>now</latest>
        </search>
        <option name="count">100</option>
        <option name="dataOverlayMode">none</option>
        <option name="drilldown">row</option>
        <option name="wrap">true</option>
      </table>
    </panel>
  </row>
</dashboard>
